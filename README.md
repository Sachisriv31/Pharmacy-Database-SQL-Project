# Pharmacy-Database-SQL-Project
Mini SQL-based pharmacy database project with schema, data, queries, and trigger

Pharmacy Management SQL Project

This is a sample SQL-based pharmacy database system. It includes:
- Table schema design with foreign key relationships
- Insertions with real-world-like data
- Select, Update, Delete, Join, Aggregate queries
- Trigger to auto-update stock quantity

## 📁 Files

- `schema.sql`: Table creation scripts
- `data.sql`: Insertion scripts
- `queries.sql`: Various queries (select, update, delete, joins, aggregation)
- `trigger.sql`: Trigger to manage inventory
- `drop_tables.sql`: Drop all tables

## ✅ Skills Demonstrated

- Relational DB Design (Normalization, PK, FK)
- SQL queries (DML, DDL)
- Joins and Aggregation
- Triggers and Procedural SQL

## 🔧 Tools Used

- Oracle SQL / MySQL
- SQL Developer or MySQL Workbench

## 📌 How to Use

1. Run `schema.sql`
2. Run `data.sql`
3. Run `queries.sql` for examples
4. Run `trigger.sql` to enable auto-update stock trigger

